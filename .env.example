# Copy to .env and fill in your key. The key stays server-side (the browser proxies through Express).
ANTHROPIC_API_KEY=sk-ant-...
# Optional: override the model used for task breakdown.
BLOOM_MODEL=claude-opus-4-8
# Optional: override the proxy port (must match vite.config.ts proxy target).
PORT=8787
